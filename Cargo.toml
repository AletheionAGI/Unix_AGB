[package]
name = "unix-agb"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
description = "Audit-only Unix-AGB Gate 0 runtime skeleton"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
libc = "0.2"

[dev-dependencies]
tempfile = "3"
